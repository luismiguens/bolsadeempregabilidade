{% extends 'base_admin.html.twig' %}

{% block body %}
    <h1>Background list</h1>


    <ul class="list-inline">
        <li  class="list-inline-item">
            <a class="btn btn-primary" href="{{ path('admin_background_new') }}">Create a new background</a>
        </li>
    </ul>


    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Year</th>
                <th>Image</th>
                    {#<th>Createdat</th>#}
                    {# <th>Updatedat</th>#}

                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for background in backgrounds %}
                <tr>
                    <td>{{ background.id }}</td>
                    <td>{{ background.year }}</td>
                    <td><img style="width: 100px" src="{{ vich_uploader_asset(background, 'imageFile') }}" alt="{{background.image }}" /></td>
                        {#<td>{{ photo.createdAt }}</td>#}
                        {#<td>{% if photo.updatedAt %}{{ photo.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>#}

                    <td>
                        <ul  class="list-inline">
                            {#<li>
                                <a href="{{ path('admin_photo_show', { 'id': photo.id }) }}">show</a>
                            </li>#}
                            <li class="list-inline-item">
                                <a class="btn btn-outline-primary" href="{{ path('admin_background_edit', { 'id': background.id }) }}">edit</a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>




    <ul class="list-inline">
        <li  class="list-inline-item">
            <a class="btn btn-primary" href="{{ path('admin_background_new') }}">Create a new background</a>
        </li>
    </ul>


{% endblock %}


