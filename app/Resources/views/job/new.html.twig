{% extends 'base_user.html.twig' %}

{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}

{% block body %}
    <h1>Criar Oferta</h1>
    <hr/>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        
        
        <div class="form-group row">
        <label class="col-form-label col-sm-2 required" for=""></label>
        <div class="col-sm-10">
            <input type="submit" class="btn btn-primary" value="Criar Oferta" />
        </div>
    </div>
        
    {{ form_end(form) }}


        
         <ul  class="list-inline">
        <li  class="list-inline-item">
            <a class="btn btn-secondary" href="{{ path('admin_job_index', { 'year': 2020 }) }}">Voltar a Lista de Ofertas</a>
        </li>
    </ul>
            
            
{% endblock %}
