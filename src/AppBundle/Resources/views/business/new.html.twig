{% extends 'base.html.twig' %}

{% block body %}

    {% include 'default/_header.html.twig'%}

    <div class="container-fluid">
        <div class="row">

            {% include 'default/_sidebar_admin.html.twig'%}

            <div class="col-sm-9 col-md-10 main">
                <h2 class="sub-header">Business create</h2>
                <div class="table-responsive">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Create" />
                    {{ form_end(form) }}

                    <ul>
                        <li>
                            <a href="{{ path('admin_business_index') }}">Back to the list</a>
                        </li>
                    </ul>

                </div>
            </div>
        </div>
    </div>
    
    {% include 'default/_footer.html.twig'%}
    

{% endblock %}


